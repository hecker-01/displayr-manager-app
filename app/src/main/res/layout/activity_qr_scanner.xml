<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent" android:layout_height="match_parent" android:background="#000000">

    <androidx.camera.view.PreviewView android:id="@+id/cameraPreview" android:layout_width="match_parent" android:layout_height="match_parent" />

    <!-- Dim overlay -->
    <View android:layout_width="match_parent" android:layout_height="match_parent" android:background="#44000000" />

    <!-- Viewfinder frame -->
    <View android:layout_width="240dp" android:layout_height="240dp" android:layout_gravity="center" android:background="@drawable/qr_viewfinder" />

    <!-- Instruction text -->
    <TextView android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_gravity="top" android:paddingTop="80dp" android:gravity="center" android:text="@string/qr_scanner_instruction" android:textAppearance="?attr/textAppearanceBodyLarge" android:textColor="#DDFFFFFF" />

    <!-- Bottom bar -->
    <com.google.android.material.button.MaterialButton android:id="@+id/enterManuallyButton" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_gravity="bottom|center_horizontal" android:layout_marginBottom="48dp" android:text="@string/qr_enter_manually" style="@style/Widget.Material3.Button.TextButton" android:textColor="#FFFFFF" app:rippleColor="#33FFFFFF" />

</FrameLayout>
